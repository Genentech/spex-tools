from setuptools import setup, find_packages

setup(
    name="hello_worker",
    version="0.1.0",
    description="Isolated Hello World worker library",
    author="Your Name",
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.6',
)
